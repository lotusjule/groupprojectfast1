---
title: "Group Project (Fast Campus)"
author: "Jiwon Kim"
date: '2018-08-06'
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r include=FALSE}
library(leaflet)
library(dplyr)
library(ggplot2)
library(lubridate)
library(anytime)
library(timeDate)
library(stringr)
library(readxl)
library(funModeling)
library(tidyr)
library(data.table)
library(DT)
```

# 연구 주제 및 연구 목적 
## 서울시 공공자전거

#### Airbnb, Uber 등과 같은 공유경제모델 색깔을 띈 회사들이 많이 나와 국민들의 관심 또한 커져 범정부적 차원에서도 공공 welfare를 위한 많은 프로그램들이 나왔다. 
<br> 

#### 예를들면 서울시 정부차원에서 만든 


## 변수 설명 
```{r variables, echo = FALSE}
variabledes <- readxl::read_xlsx("variables.xlsx", sheet = 1, col_names = TRUE)
datatable(variabledes,
          filter = 'top', options = list(
            pageLength = 8, autoWidth = TRUE
          ))
```



```{r cars}
summary(cars)
```

## Including Plots

You can also embed plots, for example:

```{r pressure, echo=FALSE}
plot(pressure)
```

